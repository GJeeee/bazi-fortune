name = "bazi-fortune-ai"
main = "index.js"
compatibility_date = "2024-06-01"

# 部署后在 Dashboard → Settings → Variables 添加 Secret：
# DEEPSEEK_API_KEY = sk-你的密钥
#
# 本地调试：复制 .dev.vars.example 为 .dev.vars 并填入 Key
# npx wrangler dev
